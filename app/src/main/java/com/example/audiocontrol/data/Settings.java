package com.example.audiocontrol.data;

public class Settings {
    public boolean isDarkThemeEnabled = true;

    public Settings(boolean isDarkThemeEnabled) {
        this.isDarkThemeEnabled = isDarkThemeEnabled;
    }

    public Settings() {}
}
